function cryptoSquare(words){
  word.split('');

  return
}

$(document).ready(function(){
  $("form#transportation_survey").submit(function(event){
      var input = $("#sentence").val();
      var input = romanIn(input);
    console.log(input);
    event.preventDefault();
    });
  });
